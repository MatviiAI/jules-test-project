const { calculateArea } = require('../src/rectangle.js');

// Test case 1: Verify that calculateArea(5, 10) returns 50
console.assert(calculateArea(5, 10) === 50, 'Test Case 1 Failed: 5 * 10 should be 50');

console.log('Rectangle area test completed.');
